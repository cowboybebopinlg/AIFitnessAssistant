/**
 * @file This file acts as a wrapper for the Dashboard component.
 * Its primary purpose is to re-export the Dashboard component, which can be useful
 * for organizing imports or for code-splitting configurations like lazy loading.
 */
import { Dashboard } from './Dashboard';

export default Dashboard;
